# Normalize line endings to LF in the repository (consistent across OSes).
* text=auto eol=lf

# Treat binary assets as binary (no EOL conversion / diffing).
*.woff2 binary
*.png binary
*.jpg binary
*.ico binary
